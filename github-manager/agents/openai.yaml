interface:
  display_name: "GitHub Manager"
  short_description: "自动检测并发布或恢复 Skills"
  default_prompt: "Use $github-manager to check changes first, then handle my skills request"

policy:
  allow_implicit_invocation: false
